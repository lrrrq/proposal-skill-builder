# diff_report 模板（v0.2 必出文档）

> **来源**：`reports/huayuquan-doc-2026-07/diff-report-v1-vs-v2.md` + `skill-v0.2-changelog-confirmed.md §4 经验 6+7`
> **适用**：当用户对 agent 生成的 PPT (v1) 做手工微调 (v2) 后，必须产出此文档

---

# <Brief 标题> Deck Diff 报告：v1 (agent 生成) vs v2 (人工微调)

**对比时间**: YYYY-MM-DD
**对比文件**:
- v1: `<agent 生成的 pptx 路径>` (<大小> / <页数>页)
- v2: `<用户微调后的 pptx 路径>` (<大小> / <页数>页)

**核心事实**:
- v2 由 `<lastModifiedBy>` 用 **<工具>** 修改
- v2 修改时间: `<ISO timestamp>`
- v1 / v2 主题（theme1.xml）<相同 / 不同>
- 所有图片 MD5 <完全一致 / 有差异>

---

## 一、<分类 1> 微调（<N> 处）

### 1.1 <具体差异点>

| 维度 | v1 | v2 |
|------|----|----|
| <属性 1> | <v1 值> | <v2 值> |
| <属性 2> | <v1 值> | <v2 值> |

**解读**: <用户决策原因>

### 1.2 <具体差异点>

...

---

## 二、<分类 2> 微调（<N> 处）

...

---

## 三、<分类 N> 微调（<N> 处）

...

---

## 四、设计意图总结（用户视角）

用户在 <工具> 里改了 <总时长>，主要做三件事：

### ① <意图 1> — <动作>

- <具体改动>

### ② <意图 2> — <动作>

- <具体改动>

### ③ <意图 3> — <动作>

- <具体改动>

---

## 五、Skill v<next> 修改思路（先不改，给你确认）

按上面的 diff 总结，proposal-reference-transfer skill v<next> 要改 <N> 个方向：

### 5.1 <方向 1>

| 元素 | 当前状态 | v<next> 改法 |
|------|---------|----------|
| <元素> | <状态> | <改法> |

### 5.2 <方向 2>

...

---

## 六、待你确认

要不要现在按 5.1-5.<N> 把 **proposal-reference-transfer skill v<next>** 写出来？还是你想先**针对某一类**单独深入讨论？

如果要改，建议分两步：
1. **先改 SKILL.md 的 hard rule 段**（v<current> → v<next> 主要 PR）— 改完下次跑就能用上
2. **再改 brand-style-pack/<company>/ 的 yaml**（如果存在的话）固化字段